export default class {
    constructor(params) {
        this.params = params;
    }

    setTitle(title) {
        document.title = title + " | HODLE - The daily NFT & crypto game.";
    }

    async getHtml() {
      return "";
    }
}
